import React from "react";

// PUBLIC_INTERFACE
export default function Footer() {
  return (
    <footer className="Footer">
      Built with a retro Neon Violet theme. Engine supports legal moves, check/checkmate/stalemate, castling, en passant,
      promotion, undo/redo, save/load, clock, and optional online play.
    </footer>
  );
}
