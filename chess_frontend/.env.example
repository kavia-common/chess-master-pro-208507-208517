# Frontend runtime configuration (Create React App)
# Copy to .env locally if needed. Do NOT commit secrets.

# Base URL for backend REST calls (optional).
# Example: http://localhost:3001
REACT_APP_API_BASE_URL=

# Base URL for backend WebSocket (optional).
# Example: ws://localhost:3001
REACT_APP_WS_URL=
